# Scheduling persistence

# PERSISTENCE_KEY = "running_schedules"

# def get_schedules():
#     schedules = open PERSISTENCE_KEY
#     for each schedule in schedules:
#         is not empty:
#             is not running already:
#                 has time run out?
#                     close item if close enough in time ???
#                 else:
#                     start watcher again, assume restart.
#                     restart_schedule(schedule_id)


# def restart_scheduler(schedule_id):
